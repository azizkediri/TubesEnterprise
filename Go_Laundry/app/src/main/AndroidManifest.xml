<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abdazizs.go_loundry">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin.AdminHomeActivity" />
        <activity android:name=".Customer.CustomerHomeActivity" />
        <activity android:name=".Admin.EditOrderActivity" />
        <activity android:name=".Admin.ListOrderAdminActivity" />
        <activity android:name=".Customer.CustomerPesanActivity" />
        <activity android:name=".Customer.TambahCustomerAcivity" />
        <activity android:name=".Customer.CustomerCekActivity" />
    </application>

</manifest>